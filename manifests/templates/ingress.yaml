---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: homework                  # Ingress name
  labels:
    app: homework
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "homework.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: homework               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
